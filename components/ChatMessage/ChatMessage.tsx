import { FC } from "react";

// Helpers
import { MessageProps } from "./helpers";

export const ChatMessage: FC<MessageProps> = ({ text, timestamp, creatorId, dateCreated, readableDate, imagePath }) => {
  return (
    <h2>text</h2>
  )
}
